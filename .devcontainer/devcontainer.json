{
  "name": "Web3 Dev (Hardhat + Viem)",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:24",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "24"
    }
  },
//"postCreateCommand": "mkdir -p backend frontend && cd backend && [ ! -f package.json ] && npm init -y && npm install --save-dev hardhat@latest dotenv@latest && touch .env && cd ../frontend && [ ! -f package.json ] && rm -rf node_modules package.json package-lock.json && CI=1 npm create vite@latest . -- --template react-ts && npm install viem@latest wagmi@latest @tanstack/react-query@lates",
  "postCreateCommand": "bash .devcontainer/init.sh",
  "forwardPorts": [
    5173,  // Vite (frontend) - always needed
    8545   // Hardhat node (backend) - optional, useful if you want to connect with MetaMask from outside
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "nomicfoundation.hardhat-solidity",
        "eamodio.gitlens",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "github.copilot",
        "github.copilot-chat",
        "dsznajder.es7-react-js-snippets",
        "bradlc.vscode-tailwindcss",
        "formulahendry.auto-rename-tag",
        "ms-vscode.vscode-typescript-next"
      ]
    }
  }
}